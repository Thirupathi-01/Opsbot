# Opsbot
Currently the opsbot.py get 20 random information which are hardcoded and write it in the file named server.log
The it will get use the string methods to get check whether the keywords like "CRITICAL,ERROR,FAILED" present in the line and it will update the count of the type of message it was.
Then while comparing if it is critical it will add it to the serperate new file as a security report.
